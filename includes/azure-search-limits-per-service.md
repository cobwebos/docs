存储受磁盘空间限制，或者受索引或文档的*最大数目*的硬性限制，具体取决于哪一个限制先实施。

| 资源 | 免费 | 基本 | S1 | S2 | S3 | S3 HD |
| --- | --- | --- | --- | --- | --- | --- |
| 服务级别协议 (SLA) |否 <sup>1</sup> |是 |是 |是 |是 |是 |
| 每个分区的存储 |50 MB |2 GB |25 GB |100 GB |200 GB |200 GB |
| 每个服务的分区数 |不适用 |1 |12 |12 |12 |3 <sup>2</sup> |
| 分区大小 |不适用 |2 GB |25 GB |100 GB |200 GB |200 GB |
| 副本 |不适用 |3 |12 |12 |12 |12 |
| 最大索引数 |3 |5 <sup>3</sup>|50 |200 |200 |每个分区 1000，或者每个服务 3000 |
| 最大索引器数 |3 |5 <sup>3</sup>|50 |200 |200 |无索引器支持 |
| 最大数据源数 |3 |5 <sup>3</sup>|50 |200 |200 |无索引器支持 |
| 最大文档数 <sup>3</sup> |10,000 |1 百万 |每个分区 1500 万，或者每个服务 1 亿 8 千万 |每个分区 6 千万，或者每个服务 7 亿 2 千万 |每个分区 1 亿 2 千万，或者每个服务 14 亿 |每个索引 1 百万，或者每个分区 2 亿 |

<sup>1</sup> 免费层和预览功能不带服务级别协议 (SLA)。 对于所有可计费的层，SLA 将在用户为服务提供足够冗余时生效。 查询（读取）SLA 需要两个或多个副本。 查询和索引（读-写）SLA 需要不少于三个副本。 分区数不属于 SLA 相关考虑因素。 

<sup>2</sup> S3 HD 的硬性限制为 3 个分区，低于 S3 的分区限制。 施加的分区限制较低是因为 S3 HD 的索引计数要高得多。 由于计算资源（存储和处理）和内容（索引和文档）都存在服务限制，因此会先达到内容限制。

>[!Important]
> **<sup>3</sup>** 从 2017 年底开始，使用功能更强大的基础硬件配置预配新创建的 Azure 搜索服务，从而允许在某些区域（巴西南部、加拿大中部、印度中部、美国东部、美国中北部、欧洲北部、美国中南部、东南亚、英国南部、西欧和美国西部）中更改一些限制：
>
>* 2017 年底后创建的基本和标准层搜索服务对文档计数没有任何限制；仅对这些服务强制实施存储限制。 
>* 对于 2017 年底后创建的 S3 高密度服务，已去除每个分区 2 亿个文档的限制，但每个索引 100 万个文档的限制保留。
>* 2017 年底后创建的基本服务增加了限制，即 15 个索引、数据源和索引器。
>
>若要详细了解哪些限制适用于现有服务，请使用 Azure 门户在服务的“概述”页上查看限制信息。