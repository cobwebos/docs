## <a name="clean-up-resources"></a>清理资源

在前面的步骤中，你将创建资源组中的 Azure 资源。 如果你不想在将来需要这些资源，则可以通过删除资源组来删除它们。
 
从 Azure 门户的左侧菜单上，选择**资源组**，然后选择**myResourceGroup**。

在资源组页中，确保列出的资源是你想要删除的。

选择**删除**，类型**myResourceGroup**在文本框中，然后选择**删除**。
