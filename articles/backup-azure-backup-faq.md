<properties
   pageTitle="Azure 备份 - 常见问题"
   description="有关 Azure 备份服务的常见问题"
   services="backup"
   documentationCenter=""
   authors="prvijay"
   manager="shreeshd"
   editor=""/>

<tags
   ms.service="backup"
   ms.workload="storage-backup-recovery"
	 ms.tgt_pltfrm="na"
	 ms.devlang="na"
	 ms.topic="article"
	 ms.date="03/26/2015"
	 ms.author="prvijay"/>

# Azure 备份 - 常见问题
下面是与 Azure 备份有关的常见问题的列表。如果你有与 Azure 备份有关的任何其他问题，请转到[论坛](https://social.msdn.microsoft.com/forums/azure/zh-CN/home?forum=windowsazureonlinebackup)并发布你的问题。来自我们社区的人员将帮助你回答问题。如果某个问题是常见问题，我们会将该问题添加到本文中，以便可以轻松快捷地找到该问题。

## 安装和配置

**Q1.我可以在哪些受支持的操作系统上使用 Azure 备份向 Azure 备份数据？** <br/>
A1.下面是 Azure 备份支持的操作系统列表


| 操作系统        | 平台           | SKU  |
| :------------- |-------------| :-----|
| Windows 8 和最新的 SP      | 64 位 | Enterprise、Pro|
| Windows 7 和最新的 SP      | 64 位 | Ultimate、Enterprise、Professional、Home Premium、Home Basic、Starter |
| Windows 8.1 和最新的 SP      | 64 位 | Enterprise、Pro|
|Windows Server 2012 R2 和最新的 SP|	64 位|	Standard、Datacenter、Foundation|
|Windows Server 2012 和最新的 SP|	64 位|	Datacenter、Foundation、Standard|
|Windows Storage Server 2012 R2 和最新的 SP	|64 位|	Standard、Workgroup|
|Windows Storage Server 2012 和最新的 SP	|64 位	|Standard、Workgroup
|Windows Server 2012 R2 和最新的 SP	|64 位|	Essential|
|Windows Server 2008 R2 SP1	|64 位|	Standard、Enterprise、Datacenter、Foundation|
|Windows Server 2008 SP2	|64 位|	Standard、Enterprise、Datacenter、Foundation|

**Q2.我可以在哪里下载最新的 Azure 备份代理？** <br/>
A2.可以从[此处](http://aka.ms/azurebackup_agent)下载最新的代理。该代理可安装在 Windows Server、SCDPM 服务器或 Windows 客户端上

**Q3.支持哪个版本的 SCDPM 服务器？** <br/>
A3.建议在最新的 SCDPM 更新汇总版本（截至 2015 年 2 月为 UR5）上安装[最新的](http://aka.ms/azurebackup_agent) Azure 备份代理

**Q4.在配置 Azure 备份代理时，系统提示我输入"保管库凭据"。保管库凭据附带任何过期日期吗？** <br/>
A4.是的，保管库凭据在 48 小时后过期。如果该文件已过期，请登录到 Azure 门户并从你的备份保管库下载保管库凭据文件。

**Q5.在每个 Azure 订阅中可以创建的备份保管库数量是否有任何限制？** <br/>
A5.是的。截至 2015 年 3 月，你可以在每个订阅中创建 25 个保管库。如果你需要更多的保管库，请创建新的订阅。

**Q6.我是否应将保管库看作一个计费实体？** <br/>
A6.虽然每个保管库中获取详细的清单，但强烈建议你考虑作为计费实体的 Azure 订阅。它在所有服务均保持一致，并且是更轻松地管理。

**Q7.可针对每个保管库注册的服务器/计算机数量是否有任何限制？** <br/>
A7.是的，最多可为每个保管库注册 50 个计算机。如果你需要注册更多的计算机，请创建新的保管库。

**Q8.可从 Windows 服务器/客户端或 SCDPM 服务器备份的数据量是否有任何限制？** <br/>
A8.没有。

**Q9.如何将我的服务器注册到其他数据中心？**<br/>
A9.通常，备份数据发送到它所注册到的备份服务的数据中心。更改数据中心的最简便方法是卸载代理，然后将代理安装并注册到新的数据中心。

**Q10.如果我重命名了用于将数据备份到 Azure 的 Windows 服务器，会发生什么情况？** <br/>
A10.将停止任何当前配置的备份。你将需要向备份保管库注册该服务器，并且该服务器将被恢复服务视为新的服务器，因此，在注册后发生的第一个备份操作将是在备份中包含的所有数据的完整备份，而不是只备份自上次备份后发生的更改。但是，如果你需要执行某一恢复操作，则可以使用"从其他服务器恢复"恢复选项来恢复已备份的数据。有关详细信息，请参阅"重命名服务器"。

**Q11.可以从哪些类型的驱动器备份文件和文件夹？** <br/>
A11.可备份下面一组驱动器/卷：

+ 可移动介质：驱动器必须报告为固定的，以便用作备份项的源。

+ 只读卷：为使卷影复制服务 (VSS) 起作用，卷必须是可写的。

+ 脱机卷	：为使 VSS 起作用，卷必须是联机的。

+ 网络共享：若要使用联机备份进行备份，卷对于服务器而言必须是本地的。

**Q12.可以从我的服务器备份哪些文件和文件夹类型？**<br/>
A12.支持以下类型：

+ 加密

+ 压缩

+ 稀疏

+ 压缩 + 稀疏

+ 硬链接：不支持，跳过

+ 重分析点：不支持，跳过

+ 加密 + 压缩：不支持，跳过

+ 加密 + 稀疏：不支持，跳过

+ 压缩流：不支持，跳过

+ 稀疏流：不支持，跳过

**Q13.针对缓存文件夹的最小大小要求是什么？** <br/>
A13.缓存文件夹的大小由你正在备份的数据量确定。通常，你应该为缓存文件夹分配数据存储所需空间的 10-15%。

**Q14.如何隔离特定服务器的数据，使其不被组织中的其他服务器恢复？ **<br/>
A14.使用同一个保管库注册的任何服务器将能够恢复由使用同一通行短语的其他服务器备份的数据。如果对于你的服务器，你希望确保只在组织中的特定服务器上发生恢复，则应使用为这些服务器指定的单独通行短语。例如，人力资源服务器可能使用一个加密通行短语，会计结算服务器使用另一个通行短语，而存储服务器使用第三个通行短语。

**Q15.是否可以在订阅之间"迁移"我的备份数据？** <br/>
A15.否

**Q16：是否可以在订阅之间"迁移"我的备份保管库？** <br/>
A16：不可以。保管库是在订阅级别创建的，在创建后无法重新分配到另一订阅。

## 备份和保留期
**Q1.要备份的每个数据源大小是否有限制？** <br/>
A1.截至 2015 年 3 月，每个数据源应小于或等于 1.7 TB。数据源是

+ 文件/文件夹卷

+ SQL DB

+ Sharepoint 场

+ Exchange Server

+ Hyper-V VM

**Q2.每天计划备份的次数是否有限制？**<br/>
A2.是的，Azure 备份支持每天通过 Windows 服务器/客户端计划 3 个备份副本，每天通过 SCDPM 计划 2 个备份副本。

**Q3.DPM 和 Azure 备份（即，在不带 DPM 的 Windows Server 上）的备份计划策略是否有差别？** <br/>
A3.是的。使用 DPM，你可以指定从（无需 DPM) 的 Windows 服务器中计划每天、每周、每月、每年，你可以指定仅每日、每周计划。

**Q4.DPM 和 Azure 备份（即，在不带 DPM 的 Windows Server 上）的保留策略是否有差别？**<br/>
A4.没有，功能是相同的。你可以指定每日、每周、每月和每年保留策略。

**Q5.是否可以选择性地配置我的保留策略 - 例如，配置每周和每日保留策略，但不配置每年和每月保留策略？**<br/>
A5.你完全可以根据自己的法规遵从/数据保留要求灵活配置策略。

**Q6.我是否可以计划下午 6 点的备份，同时指定不同时间的保留策略？**<br/>
A6.不可以。只能在备份时间点应用保留策略。在下图中，保留策略是针对上午 12 和下午 6 点生成的备份指定的。 <br/>

![Schedule Backup and Retention][1]
<br/>

**Q7.是否为计划的保留策略传输增量复制？** <br/>
A7.否，增量复制是根据备份计划页中提到的时间发送的。可以保留的时间点是根据保留策略确定的。

**Q8.如果备份保留了很长时间，是否需要很长时间才能恢复数据（比如说，恢复到最早的时间点）？** <br/>
A8.否 - 恢复最早或最晚时间点所需的时间是相同的。每个恢复点的行为类似一个完整的点。

**Q9.如果每个恢复点相当于完整的点，它会影响总体可计费备份存储吗？**<br/>
A9.典型的长期保留点产品将备份数据存储为完整的点。这些存储的效率低下，但使还原变得更方便和快速。增量复制是高效存储，但要求你还原数据链，这会影响恢复时间。Azure 备份独一无二的存储体系结构为你提供这两个领域的最佳产品，它以最佳方式将用于快速恢复的数据存储中，产生较低的存储成本。这种方法可确保提高（入口和出口）带宽使用效率，存储空间将保持为最小，恢复所用的时间将保持为最小。

**Q10.可创建的恢复点数量是否有限制？**<br/>
A10.截至 2015 年 4 月，最多可以创建 366 个恢复点。可以使用任意排列来实现此数字小于 366。例如 - 下图中的保留点数量累计为 354 个。 <br/>

![保留屏幕][2]

**Q11.Microsoft 改进了 366 数量限制后，我是否需要升级代理或重设初始备份的种子？** <br/>
A11.不需要。在我们以对服务做出更改后，你将会通过我们的社交媒体渠道（博客、Azure 公告、门户，等等）收到通知。然后，你只需要根据需求更改保留策略。

**Q12.为什么在备份中传输的数据量与我备份的数据量不相等？**<br/>
A12.备份的所有数据在传输之前都会经过压缩和加密。根据所备份的数据类型，你应该会获得 30-40% 压缩率。

## 恢复
**Q1.对于已备份到 Azure 的数据，我可以执行多少次恢复？**<br/>
A1.从 Azure 备份执行恢复的次数没有限制。

**Q2.我是否必须在恢复过程中为从 Azure 数据中心的传出流量付费？**<br/>
A2.不用。恢复是免费的，不收取传出流量费。

## "安全"
**Q1.发送到 Azure 的数据会加密吗？** <br/>
A1.是的。数据将在本地 SCDPM 服务器/客户端/SCDPM 计算机上使用 AES256 加密，并通过安全的 HTTPS 链接发送。

**Q2.Azure 中的备份数据也会加密吗？**<br/>
A2.是的。发送到 Azure 的数据将保持加密（静态加密）。Microsoft 不会解密任何位置的备份数据。

**Q3.用于加密备份数据的加密密钥的最小长度是多少？** <br/>
A3.加密密钥应至少为 16 个字符。

**Q4.如果我丢失了加密密钥，会发生什么情况？我是否可以恢复数据（或者）Microsoft 是否可以恢复数据？** <br/>
A4.用于加密备份数据的密钥只能放置在客户场地。Microsoft 不会在 Azure 中保留副本，并且无权访问密钥。如果客户丢失了密钥，Microsoft 将无法恢复备份的数据。
 


<!--Image references-->
[1]: ./media/backup-azure-backup-faq/Schedule.png
[2]: ./media/backup-azure-backup-faq/RetentionScreen1.png

<!---HONumber=56-->